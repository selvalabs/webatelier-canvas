# Third-party notices

O runtime usa o pacote `moveable`, distribuído sob licença MIT.

As dependências completas e suas versões são registradas em `editor-runtime/package-lock.json` e no lockfile gerado pelo `uv` no ambiente de desenvolvimento.
